# 4주 헬스 코치 (스마트폰 앱)

헬스클럽 4주 운동표(1~4주차, 월~금)를 그대로 실행하는 웹앱이다. 이어폰을 끼고 실행하면
순서·세트·휴식·격려를 음성으로 안내하므로 표를 보지 않고도 따라 할 수 있다.

- 위치: `public/gym/`
- 접속: 서버 실행 후 `http://<주소>/gym/` (기존 `express.static("public")`이 그대로 서비스한다)
- 스마트폰에서 접속 → 브라우저 메뉴의 **홈 화면에 추가**를 누르면 전체화면 앱처럼 실행된다(PWA).
- 서비스 워커가 앱 파일을 캐시하므로 지하 헬스장처럼 신호가 약한 곳에서도 동작한다.

## 스마트폰에 설치하기

앱스토어·플레이스토어에 올리는 네이티브 앱이 아니라 **PWA**다. 주소만 있으면 설치되고,
한 번 열어 두면 서비스 워커가 파일을 캐시해 인터넷 없이도 실행된다.

### 1단계 — GitHub Pages 켜기 (한 번만)

서버가 필요 없고, 잠들지 않으며, 무료다. `gh-pages` 브랜치에 앱 파일이 루트로 올라가 있다
(`public/gym`의 내용만 담은 배포 전용 브랜치이며, 수업 설계 코치는 서버가 필요하므로 넣지 않았다).

1. GitHub 저장소 → **Settings → Pages**
2. **Source: `Deploy from a branch`**
3. **Branch: `gh-pages`**, 폴더 **`/ (root)`** → **Save**
4. 1~2분 뒤 주소: **`https://leesleek.github.io/PT-lesson-coach/`**

저장소 이름을 바꾸면 이 주소도 함께 바뀐다(`https://leesleek.github.io/<저장소 이름>/`).
앱 파일은 모두 상대 경로를 쓰고 서비스 워커도 등록 범위를 기준으로 동작하므로, 이름이 바뀌어도
코드는 고칠 것이 없다. 다만 **이전 주소로 홈 화면에 추가해 두었다면 삭제하고 새 주소로 다시 추가**한다.
운동 기록은 경로가 아니라 도메인(`leesleek.github.io`) 단위로 저장되므로 그대로 남는다.

`main` 병합이 필요 없고, Pages가 실제로 서비스하는 구조로 오프라인 실행까지 확인했다.

**앱을 수정한 뒤 다시 배포할 때** — `public/gym/`을 고쳐 커밋한 다음 한 줄만 실행한다.

```bash
git subtree push --prefix public/gym origin gh-pages
```

> 참고 — 나중에 이 브랜치를 `main`에 병합하면 `render.yaml`의 `autoDeploy: true` 때문에
> Render에도 배포되어 `https://<Render 주소>/gym/`으로도 열린다(정적 파일이라 로그인과 무관).
> 다만 무료 플랜은 15분쯤 접속이 없으면 잠들어 첫 접속이 40~60초 걸린다. Pages 주소를 쓰는 편이 낫다.

### 2단계 — 홈 화면에 추가

**안드로이드 (Chrome)**

1. Chrome으로 위 주소를 연다.
2. 오른쪽 위 `⋮` → **앱 설치** 또는 **홈 화면에 추가** → 설치
3. 홈 화면 아이콘으로 실행하면 주소창 없는 전체화면으로 열린다.

**아이폰 (Safari — 반드시 Safari)**

1. Safari로 위 주소를 연다.
2. 아래 **공유 버튼**(↑) → **홈 화면에 추가** → 추가
3. 홈 화면 아이콘으로 실행한다.

### 3단계 — 첫 실행 때 할 일

1. Wi-Fi나 데이터가 연결된 상태에서 한 번 열어 파일을 캐시한다(이후 오프라인 실행 가능).
2. **설정 → 음성 테스트**로 이어폰 볼륨과 속도를 맞춘다.
   iOS는 화면을 처음 터치한 뒤에야 소리가 나오므로, 이 버튼을 한 번 눌러 주는 것이 좋다.
3. 아이폰은 **설정 → 화면 항상 켜기**를 켠다(화면이 꺼지면 iOS가 음성 합성을 멈춘다).
   이 기능은 iOS 16.4 이상에서 동작한다.
4. 무음 모드를 해제한다. 진동은 안드로이드에서만 동작한다.

### 참고 — 집 안에서 먼저 시험해 보려면

`npm start` 후 같은 Wi-Fi의 휴대폰에서 `http://<PC 내부IP>:3000/gym/`으로 열면 화면과 음성은
확인할 수 있다. 다만 `http`이므로 **홈 화면 설치와 오프라인 캐시는 동작하지 않는다**
(브라우저가 보안 연결에서만 허용한다). 실제 사용은 1단계의 `https` 주소로 하는 것이 좋다.

## 화면 구성

| 탭 | 내용 |
| --- | --- |
| 오늘 | 오늘 할 세션, 주차 지침 체크리스트, 동작 순서(편집·대체·제외), 물·단백질 카운터, `운동 시작` |
| 자세 | 18개 동작의 움직이는 자세 그림, 핵심 포인트, 흔한 실수, 호흡법, 유튜브 검색 링크, 내 자세 사진 |
| 기록 | 완료 세션·연속 운동일·총 볼륨, 4주 진행표, 주차별 볼륨 그래프, 동작별 최고 무게, 백업 |
| 설정 | 음성·소리·시간·템포·화면·영양 목표 |

## 운동 진행 화면

- **음성 안내** — 동작 이름, 세트 수와 반복 횟수, 지난 기록, 휴식 시간, 남은 시간(1분/30초/10초),
  마지막 3초 카운트다운 신호음, 유산소 속도·경사, 인터벌 전환, 완료 요약을 말로 알려 준다.
- **일시정지 / 재개** — 타이머와 음성이 함께 멈춘다. `이전`, `이 동작 건너뛰기`, `±15초`도 지원한다.
- **세트 기록** — 세트마다 무게·반복을 남기면 지난 기록을 자동으로 채워 주고
  "과부하의 원리"에 맞춰 다음 목표를 제안한다.
- **격려 멘트** — 세트 시작·휴식·유산소·마지막 세트·운동 완료 상황별 문구를 무작위로 섞어 말한다
  (빈도: 끄기/적게/보통/많이).
- **자세 확인** — 진행 중 `자세 보기`로 그림과 체크 포인트를 바로 볼 수 있다.
- **스트레칭** — 표의 "스트레칭" 항목을 9단계(또는 짧게 5단계) 타이머 시퀀스로 안내한다.
- **인터벌** — 3·4주차 화·목의 `9~10km/h 2분 + 6~7km/h 2분 × 4세트`를 구간별로 자동 전환한다.

## 설정 항목

음성 사용/목소리 선택/속도/음높이/크기, 짧은 안내 모드(이어폰 배려), 남은 시간 음성 알림,
격려 멘트 빈도, 신호음, 진동, 준비 시간, 세트 간·동작 간 휴식, 자동 진행, 유산소 절반 알림,
스트레칭 길이, 반복 템포 신호음(내리기/멈추기/올리기 초), 화면 항상 켜기, 큰 글씨, 테마,
무게 단위, 하루 물·단백질 목표.

## 데이터

모든 기록은 브라우저 `localStorage`에만 저장되며 서버로 전송하지 않는다.
`기록 내보내기 / 가져오기`(JSON)로 기기를 옮길 수 있고, `일정(.ics) 내보내기`로 다음 주 월요일부터
4주간 월~금 저녁 7시 일정과 30분 전 알림을 캘린더에 등록할 수 있다.

## 파일 구조

```
public/gym/
  index.html      화면 골격
  styles.css      스타일(다크/라이트, 큰 글씨)
  app.js          화면 렌더링 + 운동 진행 엔진
  program.js      4주 × 5일 운동표 데이터
  exercises.js    동작 라이브러리(자세 그림·포인트·실수·대체 동작)
  speech.js       음성 안내·신호음·진동·격려 멘트
  store.js        설정·진행·기록 저장
  sw.js           오프라인 캐시
  manifest.webmanifest
  icon.svg, icon-maskable.svg            원본 아이콘
  icon-192.png, icon-512.png,
  icon-maskable-512.png                  안드로이드 설치용
  apple-touch-icon.png                   iOS 홈 화면용(iOS는 PNG만 인식)
```

아이콘을 바꿀 때는 `icon.svg`를 고친 뒤 같은 그림으로 PNG들을 다시 만들어야 한다.

배포 브랜치 `gh-pages`는 이 폴더의 내용만 루트에 담은 사본이므로 직접 편집하지 않는다.
항상 `public/gym/`을 고치고 `git subtree push`로 내보낸다.

운동표를 바꾸려면 `program.js`의 `WEEKS`만 수정하면 된다. 동작을 추가할 때는
`exercises.js`의 `EXERCISES`에 항목을 넣고 `pattern`으로 자세 그림 패턴을 지정한다.

## 진행 방식과 휴식일

프로그램은 날짜가 아니라 **순서**로 진행한다(20개 세션). 힘든 날은 쉬고 다음에 이어서 하면
자동으로 다음 세션이 뜬다. 2주차 지침의 "중간에 휴식일 투입하기"를 그대로 반영한 방식이며,
`다른 날 선택`이나 기록 탭의 진행표에서 원하는 세션으로 바로 이동할 수도 있다.

## 알아둘 점

- 음성은 브라우저의 Web Speech API를 쓴다. 기기에 한국어 음성이 없으면 기본 음성으로 읽고,
  음성을 지원하지 않는 브라우저에서는 신호음과 화면 안내만 동작한다.
- iPhone은 화면이 꺼지면 음성 합성이 멈춘다. 설정의 **화면 항상 켜기**를 켜 두는 것이 좋다.
- 자세 사진은 640px JPEG로 줄여 동작마다 최대 3장까지 기기에 저장한다.
- 이 앱은 운동 안내 도구이며 의료 조언이 아니다. 통증이 있으면 중단하고 전문가와 상의한다.
